@model List<EmployeeModel>

<table class="table">
    <thead>
    <tr>
        <th scope="col">#</th>
        <th scope="col">Nom</th>
        <th scope="col">Poste</th>
        <th scope="col">Salaire</th>
        <th scope="col">Modification</th>
    </tr>
    </thead>
    <tbody>
    @{ var i = 1; }
    @foreach (var emp in Model)
    {
        <tr>
            <th scope="row">@i</th>
            <td>@emp.Name</td>
            <td>@emp.Position</td>
            <td>@emp.Salary €</td>
            <td>
                <a asp-action="Edit" asp-route-id="@emp.Id" class="btn btn-primary">Edit</a>
                <a asp-action="Delete" asp-route-id="@emp.Id" class="btn btn-primary">Suppr</a>
            </td>
        </tr>
        i++;
    }
    </tbody>
</table>